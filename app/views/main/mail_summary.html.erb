<!-- Boostrap/CSS envelope to show links from older to newer-->
<!-- this page is temporal, just to see if the mailing function is working properly -->
<section class="container-fluid" id="section2">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2 text-center">

          <h2 color="white"><% if current_user.name != nil%>
                    <%= current_user.name %>,
                    <%end%>
          Your summary e-mail was sent</h2>

       <div class="col-xs-12 col-sm-12 col-md-12 column">  

                <div class="panel panel-primary">
                  <div class="panel-heading">
                    <h3 class="panel-title">My Notes</h3>
                  </div>
                  <div class="panel-body">
                    <%= link_to "Go back home!", :root %>
                    <br>
                    <br>
                  </div>
                </div>
       </div>

    </div>
  </div>
</section>